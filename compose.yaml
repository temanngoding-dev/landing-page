services:
  web:
    build: .
    image: landing-temanngoding-dev:dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file: .env.production
    command: npm run dev

volumes:
  node_modules: